// Note: This address should be assigned to the value of address in move.toml
//       Make sure it starts with '0x';

import 0x3cf4cc0c7dcf04ff846484288a4f40ef.HelloWorld;

main() {
    let x: HelloWorld.T;
    let y: &HelloWorld.T;
    x = HelloWorld.new(5);
    y = &x;
    HelloWorld.t(move(y));
    return;
}
